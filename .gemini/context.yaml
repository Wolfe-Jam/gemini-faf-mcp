# Gemini CLI Context Configuration
# Auto-generated by FAF init - Native Google Handshake
# https://faf.one

source: project.faf
format: faf
version: "2.5.2"

# FAF provides the foundational context
# Gemini CLI reads this for project understanding
context_provider: faf
auto_sync: true
