{
  "function_declarations": [
    {
      "name": "read_project_dna",
      "description": "Read the current project DNA from the FAF Source of Truth. Use this to understand the project context, goals, stack, and current state.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "update_project_dna",
      "description": "HIGH-VALUE MUTATION: Update the project DNA via voice command. IMPORTANT: Always verbally confirm the exact changes with the user before executing, as this commits directly to GitHub via GITHUB_TOKEN. Use when the user explicitly requests changes to project focus, status, phase, goals, or other DNA fields.",
      "parameters": {
        "type": "object",
        "properties": {
          "updates": {
            "type": "object",
            "description": "Key-value pairs to update. Use dot notation for nested fields (e.g., 'state.focus', 'project.goal', 'state.phase')",
            "additionalProperties": {
              "type": "string"
            }
          },
          "message": {
            "type": "string",
            "description": "Optional commit message describing the change"
          }
        },
        "required": ["updates"]
      }
    }
  ]
}
